import {useGSAP} from "@gsap/react";

const Memories = () => {

    useGSAP(() => {
        document.fonts.ready.then(() => {
        })
    })

    return (
        <div>Memories</div>
    )
}
export default Memories
